"""
__main__.py
# TODO !!!
"""
# import math
# import random

# from tracer.defaults import *
# from tracer.camera import Ray, Camera
# from tracer.vector import Vector3


# def get_color(ray: Ray):
#     unit_direction = ray.direction.unit_vector()
#     t = 0.5 * unit_direction.y + 1.0
#     return (1.0 - t) * Vector3(1.0, 1.0, 1.0) + t * Vector3(0.5, 0.7, 1.0)


def main():
    # nx = 200
    # ny = 100
    # print(f"P3\n{nx} {ny}\n255")
    # camera = Camera()
    # for j in range(ny - 1, -1, -1):
    #     for i in range(0, nx):
    #         u = float(i + random.random()) / float(nx)
    #         v = float(j + random.random()) / float(ny)
    #         ray = camera.get_ray(u, v)
    #         color = get_color(ray)
    #         ir = int(255.99 * color.r)     # Access the R, G, B coordinates
    #         ig = int(255.99 * color.g)
    #         ib = int(255.99 * color.b)
    #         print(ir, ig, ib)
    print("__main__.py TBD")


if __name__ == '__main__':
    main()
